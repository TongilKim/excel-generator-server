{
  "name": "figma-image-proxy",
  "version": "1.0.0",
  "description": "Custom proxy server for Figma plugin to fetch images with caching",
  "main": "index.js",
  "scripts": {
    "start": "node api/server.js",
    "dev": "nodemon api/server.js"
  },
  "dependencies": {
    "node-fetch": "^2.6.7",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "engines": {
    "node": ">=14.x"
  }
}
